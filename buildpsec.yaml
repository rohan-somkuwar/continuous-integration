version: 0.2

phases:
  install:
    commands:
      - echo Installing NGINX - echo apt-get install NGINX

   build:
    commands:
      - echo Build started on date`

  post_build:
    commands:
      - echo Configuring NGINX
